

//DOM is document object model (BOM is browser object model)

//refer back to Window object within console


// within Document (contains elements and nodes), we have access to all html code
// the elements and nodes within Document are also objects

// DOM is object-oriented representation of what's going on in HTML that you can access through Document object